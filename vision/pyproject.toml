[project]
name = "jev-orb-vision"
version = "0.1.0"
description = "On-device UI element segmentation and visual grounding pipeline"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ultralytics>=8.3.0",
    "ocrmac>=1.0.0",
    "pillow>=10.0.0",
    "pyobjc-framework-Quartz>=10.0",
    "httpx>=0.27.0",
    "huggingface-hub>=0.24.0"
]
